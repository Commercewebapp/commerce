{
  "scripts": {
    "precompress": "{{ npm install nsfwjs }}",
    "precompress": "{{ npm install @tensorflow/tfjs-node }}",
    "precompress": "{{ npm install fs }}",
    "precompress": "{{ npm install axios }}"
  },
  "engine": {
      "node": "14.16.0",
      "npm": "6.14.11"
  }
}
